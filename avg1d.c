#include<Stdio.h>
void ar(int num,int numm);
main()
{
    int arr[100],c=0;
    printf("enter the number:");
    for(int i=0;i<arr;i++)
    {
    scanf("%d",&arr[i]);
    ar(arr[i]);
    sum=sum+arr[i];
    }
    ar(sum);
    
    //return sum;
}
void ar(int num,int numm);
{
    if(num>0)
    {
        z
    }
}
